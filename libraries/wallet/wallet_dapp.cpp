#include "wallet_api_impl.hpp"
#include <graphene/wallet/wallet.hpp>

namespace graphene { namespace wallet { namespace detail {
/*
signed_transaction wallet_api_impl::register_dapp( string name, account_id_type owner_account_id, dapp_options common_options )
{ try {
    
} FC_CAPTURE_AND_RETHROW( (name)(owner_account_id)(common_options) ) }

signed_transaction wallet_api_impl::register_dapp_account( account_id_type account_id, dapp_id_type dapp_id )
{ try {
    
} FC_CAPTURE_AND_RETHROW( (account_id)(dapp_id) ) }
*/
} } }